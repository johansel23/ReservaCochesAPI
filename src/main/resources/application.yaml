spring:
    datasource:
        url: jdbc:mariadb://localhost:3306/reserva_coche
        username: root
        password: 
        driver-class-name: org.mariadb.jdbc.Driver
logging:
    level:
        org:
            hibernate:
                SQL: debug
