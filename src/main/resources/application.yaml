spring:
    datasource:
        url: jdbc:mariadb://localhost:3306/reserva_coche
        username: root
        password: 
        driver-class-name: org.mariadb.jdbc.Driver
    web:
        resources:
            static-locations: 
                file: public
logging:
    level:
        org:
            hibernate:
                SQL: debug
